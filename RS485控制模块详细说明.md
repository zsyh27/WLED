# RS485控制模块详细使用说明

## 📋 配置选项详解

### 1. EchoEnabled（回声服务）
**作用**：启用RS485通信测试功能
- **启用时**：可以使用 `ECHO <text>` 命令测试RS485连接
- **禁用时**：ECHO命令将被忽略，节省处理资源
- **使用场景**：
  - 初次设置时验证RS485连接是否正常
  - 调试RS485通信问题
  - 测试命令格式和响应

**测试示例**：
```
发送: ECHO Hello World
响应: Hello World

发送: ECHO 测试中文
响应: 测试中文
```

### 2. DebugMode（调试模式）
**作用**：在串口监视器输出详细的调试信息
- **启用时**：显示所有RS485收发消息和处理过程
- **禁用时**：只输出关键错误信息
- **调试信息包括**：
  - 接收到的原始RS485数据
  - 发送的响应数据
  - 命令解析过程
  - 错误和异常情况

**调试输出示例**：
```
RS485 RX: PRESET 1
RS485 TX: OK: Preset 1 activated
RS485 Control: WLED state change - mode: 2
```

## 🔧 预设ID范围修复

### 问题原因
之前的代码限制预设ID为1-16，但WLED实际支持1-250个预设。

### 修复内容
- **旧限制**：1-16个预设
- **新限制**：1-250个预设（符合WLED标准）
- **错误消息**：从 "Invalid preset ID (1-16)" 改为 "Invalid preset ID (1-250)"

### 预设命令使用
```bash
# 激活预设（现在支持1-250）
PRESET 1      # 激活预设1
PRESET 50     # 激活预设50
PRESET 250    # 激活预设250

# 查询当前预设
PRESET?       # 返回: CURRENT: Preset X

# 错误示例
PRESET 0      # 返回: ERROR: Invalid preset ID (1-250)
PRESET 251    # 返回: ERROR: Invalid preset ID (1-250)
```

## 📡 完整命令列表

### 1. ECHO命令（需要启用EchoEnabled）
```bash
ECHO <任意文本>
```
**功能**：回声测试，返回发送的文本
**示例**：
- `ECHO test` → `test`
- `ECHO 123` → `123`
- `ECHO Hello World` → `Hello World`

### 2. PRESET命令
```bash
PRESET <1-250>    # 激活指定预设
PRESET?           # 查询当前预设
```
**功能**：控制WLED预设
**示例**：
- `PRESET 1` → `OK: Preset 1 activated`
- `PRESET 10` → `OK: Preset 10 activated`
- `PRESET?` → `CURRENT: Preset 1`

### 3. STATUS命令
```bash
STATUS
```
**功能**：查询系统状态
**响应**：`STATUS: Online, Connected: Yes/No`

### 4. HELP命令
```bash
HELP
```
**功能**：显示所有可用命令
**响应**：`COMMANDS: ECHO <text>, PRESET <1-250>, PRESET?, STATUS, HELP`

## ⚙️ 配置参数说明

### 基本配置
- **Enabled**：启用/禁用RS485控制功能
- **Baud Rate**：串口波特率（9600, 19200, 38400, 57600, 115200）
- **RX Pin**：接收数据引脚（GPIO编号）
- **TX Pin**：发送数据引脚（GPIO编号）

### 高级配置
- **Buffer Size**：缓冲区大小（256-2048字节）
- **Timeout**：超时时间（500-5000毫秒）
- **Echo Enabled**：启用回声服务进行通信测试
- **Debug Mode**：启用调试输出到串口监视器

## 🔌 硬件连接

### 推荐连接方式
```
ESP32          RS485模块
GPIO26    →    RO (接收输出)
GPIO27    →    DI (数据输入)
3.3V      →    VCC
GND       →    GND
```

### RS485模块到外部设备
```
RS485模块      外部控制器
A+        →    A+
B-        →    B-
```

## 🛠️ 故障排除

### 1. 预设激活失败
**可能原因**：
- 预设ID超出范围（必须是1-250）
- 预设在WLED中不存在
- WLED系统繁忙

**解决方法**：
1. 确认预设ID在1-250范围内
2. 在WLED网页界面中创建对应的预设
3. 启用调试模式查看详细错误信息

### 2. ECHO命令无响应
**可能原因**：
- EchoEnabled配置被禁用
- RS485硬件连接问题
- 波特率不匹配

**解决方法**：
1. 在Config→Usermods中启用EchoEnabled
2. 检查RX/TX引脚连接
3. 确认波特率设置正确

### 3. 调试信息查看
**步骤**：
1. 启用DebugMode配置
2. 连接串口监视器（波特率115200）
3. 发送RS485命令观察输出

**调试输出示例**：
```
RS485 Control: Starting setup...
RS485 Control: Serial initialized - RX:26, TX:27, Baud:9600
RS485 RX: ECHO test
RS485 TX: test
RS485 Control: Setup completed
```

## 📊 状态监控

### Info页面显示内容
- **RS485 Control Active**：模块运行状态
- **Messages RX**：接收消息计数
- **Messages TX**：发送消息计数
- **Parse Errors**：解析错误计数
- **Baud Rate**：当前波特率
- **RX Pin**：接收引脚
- **TX Pin**：发送引脚
- **Echo Service**：回声服务状态
- **Debug Mode**：调试模式状态

### 启用/禁用控制
在Info页面可以看到Enable/Disable按钮，点击可以快速启用或禁用RS485控制功能。

## 🔄 更新内容

### 本次修复
1. **扩展预设支持**：从1-16扩展到1-250
2. **改进配置说明**：添加详细的参数说明
3. **优化错误消息**：提供更准确的错误信息
4. **完善文档**：详细说明各配置选项的作用

### 固件信息
- **文件**：`build_output/release/WLED_0.16.0-alpha_ESP32.bin`
- **大小**：1.32 MB
- **支持预设**：1-250个
- **usermod数量**：2个（audioreactive + rs485_control）