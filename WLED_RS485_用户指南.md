# WLED RS485串口控制功能 - 用户指南

## 概述

WLED RS485串口控制功能允许通过485总线使用文本指令远程控制LED灯光效果。该功能作为WLED usermod实现，完全兼容现有WLED功能，不影响Web界面、WiFi连接或其他API接口。

## 硬件要求

### 必需硬件
- ESP32-WROOM-32E开发板
- RS485转TTL模块（如MAX485芯片模块）
- 连接线材

### 硬件连接
```
ESP32          RS485模块
GPIO26   -->   RO (接收输出)
GPIO27   -->   DI (数据输入)
3.3V     -->   VCC
GND      -->   GND
```

### 485总线连接
```
RS485模块      485总线
A+       -->   A+
B-       -->   B-
```

## 软件配置

### 1. 启用RS485功能

在 `wled00/my_config.h` 文件中添加：
```cpp
#define USERMOD_RS485_CONTROL
```

### 2. 编译和烧录

使用PlatformIO编译并烧录到ESP32：
```bash
pio run -e esp32dev -t upload
```

### 3. Web界面配置

1. 连接到WLED的Web界面
2. 进入"Config" -> "Usermods"
3. 找到"RS485 Control"配置项
4. 配置以下参数：
   - **启用**: 勾选启用RS485功能
   - **波特率**: 选择9600（默认）、19200、38400或57600
   - **接收引脚**: GPIO26（默认）
   - **发送引脚**: GPIO27（默认）
   - **缓冲区大小**: 512字节（默认）
   - **回显功能**: 勾选启用回显验证

## 支持的指令

### 1. 回显测试指令
用于验证通信连接是否正常。

**格式**: `ECHO <消息内容>`

**示例**:
```
ECHO Hello World
```

**响应**:
```
Hello World
```

### 2. 预设控制指令

#### 激活预设
**格式**: `PRESET SET <预设编号>`

**示例**:
```
PRESET SET 1
```

**成功响应**:
```
OK: Preset 1 activated - [预设名称]
```

**错误响应**:
```
ERROR: INVALID_PRESET - Preset 99 does not exist
USAGE: PRESET SET <1-16>
```

#### 查询当前预设
**格式**: `PRESET GET`

**示例**:
```
PRESET GET
```

**响应**:
```
CURRENT: Preset 3 - Rainbow
```

### 3. 状态查询指令
**格式**: `STATUS`

**示例**:
```
STATUS
```

**响应**:
```
STATUS: Online, Preset: 1, Brightness: 128, WiFi: Connected
```

## 使用场景

### 场景1: 单设备控制
使用USB转485适配器直接连接一个WLED控制器：
```
PC/中控 --> USB转485 --> RS485总线 --> WLED控制器
```

### 场景2: 多设备控制
通过485总线控制多个WLED控制器：
```
中控主机 --> USB转485 --> RS485总线 --> 多个WLED控制器
```

### 场景3: 自动化系统集成
将WLED控制器集成到楼宇自动化或舞台控制系统中。

## 故障排除

### 常见问题

#### 1. 无法通信
**症状**: 发送指令无响应

**解决方案**:
1. 检查硬件连接是否正确
2. 确认波特率设置一致
3. 使用ECHO指令测试基本通信
4. 检查485总线的A+/B-连接

#### 2. 指令格式错误
**症状**: 收到"INVALID_FORMAT"错误

**解决方案**:
1. 确保指令格式正确
2. 检查指令大小写（不区分大小写）
3. 确保参数之间有空格分隔

#### 3. 预设不存在
**症状**: 收到"INVALID_PRESET"错误

**解决方案**:
1. 确认预设编号在有效范围内（1-16）
2. 在WLED Web界面中检查预设是否已创建
3. 使用`PRESET GET`查询当前可用预设

#### 4. 通信不稳定
**症状**: 偶尔丢失指令或响应

**解决方案**:
1. 检查485总线的终端电阻
2. 确保总线长度在规范范围内
3. 降低波特率到9600
4. 检查电源供电稳定性

### 调试模式

启用调试模式以获取详细的通信信息：

1. 在Web界面中启用"调试模式"
2. 通过串口监视器查看调试输出：
```bash
pio device monitor -e esp32dev
```

### 日志信息

系统会记录以下类型的事件：
- 通信错误
- 指令解析错误
- 预设控制操作
- 缓冲区溢出
- 硬件故障

## 性能特性

### 资源使用
- **RAM使用**: 约2-3KB
- **Flash使用**: 约15-20KB
- **CPU影响**: 最小（每次循环<1ms）

### 通信性能
- **支持波特率**: 9600, 19200, 38400, 57600 bps
- **缓冲区大小**: 512字节（可配置）
- **响应时间**: 通常<100ms
- **并发连接**: 支持485总线上的多设备

### 兼容性
- **WLED版本**: 0.16.0及以上
- **ESP32型号**: ESP32-WROOM-32E（推荐）
- **其他功能**: 完全兼容Web界面、WiFi、API等

## 高级配置

### 自定义引脚配置
如需使用不同的GPIO引脚，在配置中修改：
- 接收引脚：可选GPIO 0-39（输入引脚）
- 发送引脚：可选GPIO 0-33（输出引脚）

### 缓冲区优化
根据应用需求调整缓冲区大小：
- 小型应用：256字节
- 标准应用：512字节（默认）
- 高负载应用：1024字节

### 超时设置
调整通信超时时间（毫秒）：
- 快速响应：500ms
- 标准设置：1000ms（默认）
- 稳定连接：2000ms

## 技术支持

### 获取帮助
1. 查看项目文档和README文件
2. 检查GitHub Issues页面
3. 参考WLED官方文档

### 报告问题
报告问题时请提供：
1. 硬件配置信息
2. 软件版本信息
3. 错误日志和调试输出
4. 重现问题的步骤

### 贡献代码
欢迎提交改进建议和代码贡献：
1. Fork项目仓库
2. 创建功能分支
3. 提交Pull Request

## 版本历史

### v1.0.0
- 初始版本发布
- 支持基本的485通信
- 实现回显和预设控制功能
- 完整的Web界面集成

---

**注意**: 本功能仍在持续开发中，欢迎反馈使用体验和改进建议。