# RS485 功能调试指南

## 问题现象
sscom 发送命令后没有响应

## 可能原因分析

### 1. Usermod 未正确初始化
**检查方法：**
- 查看串口监视器输出，应该看到以下信息：
  ```
  RS485 Control: setup() called
  RS485 Control: Config - enabled: true, rxPin: 26, txPin: 27, baudRate: 9600
  RS485 Control: Interface initialized successfully
  RS485 Control: setup() completed
  ```

**如果没有看到这些信息：**
- 检查 `wled00/my_config.h` 中是否定义了 `#define USERMOD_RS485_CONTROL`
- 检查 `wled00/my_config.h` 中是否定义了 `#define WLED_DEBUG`
- 重新编译并烧录固件

### 2. UART2 配置问题
**检查方法：**
- 使用简单的 UART 测试程序 `test_uart_basic.cpp`
- 编译并烧录测试程序，通过串口监视器查看是否能正常通信

**测试步骤：**
1. 编译并烧录 `test_uart_basic.cpp`
2. 打开串口监视器（115200 波特率）
3. 使用 sscom 发送 "TEST" 命令
4. 应该在串口监视器中看到接收和发送的消息

### 3. 硬件连接问题
**正确连接：**
- ESP32 GPIO26 (RX) ← 485转TTL TXD
- ESP32 GPIO27 (TX) → 485转TTL RXD
- 485转TTL 的 A+/B- 连接到 RS485 总线

**检查方法：**
- 使用万用表检查连接
- 确认 485转TTL 模块供电正常（通常是 3.3V 或 5V）
- 检查 RS485 总线是否有终端电阻

### 4. 波特率不匹配
**检查设置：**
- ESP32 代码：9600 波特率，8 数据位，1 停止位，无校验
- sscom 设置：9600 波特率，8 数据位，1 停止位，无校验

### 5. 调试模式未启用
**启用调试输出：**
在 `wled00/my_config.h` 中添加：
```cpp
#define WLED_DEBUG
```

## 调试步骤

### 步骤 1：验证 Usermod 加载
1. 重新编译并烧录固件
2. 打开串口监视器（115200 波特率）
3. 重启 ESP32
4. 查看是否有 RS485 相关的初始化信息

### 步骤 2：使用简单测试程序
1. 编译并烧录 `test_uart_basic.cpp`
2. 使用 sscom 发送测试命令
3. 验证基本 UART 通信是否正常

### 步骤 3：检查硬件连接
1. 使用万用表验证连接
2. 检查 485转TTL 模块状态指示灯
3. 确认供电电压正确

### 步骤 4：逐步测试
1. 先测试简单的 ECHO 命令
2. 再测试 STATUS 命令
3. 最后测试 PRESET 命令

## 常见问题解决

### 问题：串口监视器没有 RS485 初始化信息
**解决方案：**
1. 确认 `#define USERMOD_RS485_CONTROL` 已定义
2. 确认 `#define WLED_DEBUG` 已定义
3. 重新编译并烧录

### 问题：UART 测试程序也没有响应
**解决方案：**
1. 检查硬件连接
2. 检查 485转TTL 模块是否正常工作
3. 尝试交换 RX/TX 连接

### 问题：能接收但不能发送
**解决方案：**
1. 检查 TX 引脚连接
2. 检查 485转TTL 的发送使能引脚
3. 确认 RS485 总线没有冲突

### 问题：间歇性通信失败
**解决方案：**
1. 检查供电稳定性
2. 添加 RS485 总线终端电阻
3. 检查信号完整性

## 测试命令

### 基本测试命令
```
ECHO Hello
STATUS
PRESET GET
PRESET SET 1
```

### 预期响应
```
ECHO Hello -> Hello
STATUS -> RS485 Status: Connected, Messages RX:1 TX:1
PRESET GET -> No preset active (或当前预设信息)
PRESET SET 1 -> OK: Activated preset 1 (或错误信息)
```

## 联系支持
如果按照以上步骤仍无法解决问题，请提供：
1. 串口监视器的完整输出
2. 硬件连接照片
3. sscom 的设置截图
4. 使用的 485转TTL 模块型号