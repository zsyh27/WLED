# RS485 功能编译测试指南

## 测试方案

为了诊断 sscom 无响应的问题，我们提供了三个测试程序，按复杂度递增：

### 1. 基础 UART 测试 (test_uart_basic.cpp)
**目的**: 验证硬件连接和基本 UART2 功能
**特点**: 
- 独立程序，不依赖 WLED
- 直接测试 GPIO26/27 上的 UART2
- 简单的回显功能

**编译方法**:
```bash
# 使用 Arduino IDE 或 PlatformIO 编译 test_uart_basic.cpp
# 确保选择正确的 ESP32 开发板
```

### 2. 简化 Usermod 测试 (test_rs485_usermod_simple.cpp)
**目的**: 验证 usermod 加载机制和基本集成
**特点**:
- 作为 WLED usermod 运行
- 简化的 RS485 功能
- 验证 usermod 生命周期

**使用方法**:
1. 将 `test_rs485_usermod_simple.cpp` 复制到 `usermods/rs485_control/` 目录
2. 临时重命名原始的 `rs485_control.cpp` 为 `rs485_control.cpp.bak`
3. 将 `test_rs485_usermod_simple.cpp` 重命名为 `rs485_control.cpp`
4. 编译并烧录 WLED

### 3. 完整 RS485 Usermod (rs485_control.cpp)
**目的**: 完整功能测试
**特点**:
- 完整的 RS485 控制功能
- 增强的调试输出
- 所有原始功能

## 当前问题诊断

### 步骤 1: 验证基础硬件
使用 `test_uart_basic.cpp`:
1. 编译并烧录到 ESP32
2. 打开串口监视器 (115200 波特率)
3. 使用 sscom 发送 "TEST" 命令
4. 检查是否有响应

**预期结果**:
- 串口监视器显示: "Received: 'TEST'"
- sscom 收到: "Test response from ESP32"

### 步骤 2: 验证 Usermod 加载
使用 `test_rs485_usermod_simple.cpp`:
1. 按上述方法替换文件
2. 编译并烧录 WLED
3. 打开串口监视器 (115200 波特率)
4. 查看启动信息

**预期结果**:
```
TestRS485Usermod: Constructor called
Usermods setup
TestRS485Usermod: setup() called
TestRS485Usermod: UART2 initialized on GPIO26/27 @ 9600 baud
TestRS485Usermod: setup() completed
```

### 步骤 3: 测试完整功能
使用增强调试版本的 `rs485_control.cpp`:
1. 恢复原始文件名
2. 确保 `my_config.h` 中有 `#define WLED_DEBUG`
3. 编译并烧录
4. 查看详细的调试输出

**预期结果**:
```
RS485ControlUsermod: Constructor called
RS485 Control: setup() called
RS485 Control: Config - enabled: true, rxPin: 26, txPin: 27, baudRate: 9600
RS485Interface: Initializing UART2 - RX:26 TX:27 Baud:9600
RS485Interface: UART2 begin() called, checking serial object...
RS485Interface: UART2 serial object is valid
RS485 Control: Interface initialized successfully
RS485 Control: setup() completed
```

## 常见问题排查

### 问题 1: 没有看到 usermod 构造函数调用
**可能原因**:
- `#define USERMOD_RS485_CONTROL` 未定义
- 编译时未包含 usermod 文件

**解决方案**:
1. 检查 `wled00/my_config.h`
2. 确认文件路径正确
3. 清理并重新编译

### 问题 2: setup() 未被调用
**可能原因**:
- usermod 注册失败
- WLED 初始化问题

**解决方案**:
1. 检查 `REGISTER_USERMOD` 宏
2. 查看完整的启动日志
3. 确认没有编译错误

### 问题 3: UART2 初始化失败
**可能原因**:
- GPIO 引脚冲突
- 硬件问题

**解决方案**:
1. 检查引脚定义
2. 尝试不同的 GPIO 引脚
3. 验证硬件连接

## 调试输出说明

### 正常启动序列
```
1. RS485ControlUsermod: Constructor called
2. RS485ControlUsermod: Constructor completed
3. Usermods setup
4. RS485 Control: setup() called
5. RS485Interface: Initializing UART2...
6. RS485 Control: Interface initialized successfully
7. RS485 Control: setup() completed
```

### 运行时输出
```
每10秒: RS485 Control: Loop running - RX buffer: 0, TX buffer: 0
收到消息时: RS485 Control: Received message: 'ECHO test'
发送响应时: RS485 Control: Sending response: 'test'
```

## 下一步行动

根据测试结果:

1. **如果基础 UART 测试失败**: 检查硬件连接
2. **如果 usermod 未加载**: 检查编译配置
3. **如果 usermod 加载但无响应**: 检查 UART 初始化
4. **如果一切正常但仍无响应**: 检查命令格式和协议

请按顺序执行测试，并提供每个步骤的详细输出结果。