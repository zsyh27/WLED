# WLED RS485 V5 更新说明

## 📅 更新日期
2025年1月24日

## 🆕 主要更新内容

### 1. 自定义序列效果状态重置修复 ✅

#### 问题描述
- 自定义序列效果"Custom Seq 1"在每次调用预设时不会从头开始执行
- 效果会继续之前停止的状态，而不是重新开始序列

#### 解决方案
- 修复了`mode_custom_sequence_1()`函数中的状态重置逻辑
- 确保每次效果激活时都从序列开始位置重新开始
- 添加了立即视觉反馈，显示序列中的第一个LED

#### 技术细节
```cpp
if (SEGENV.call == 0) {
    SEGENV.step = strip.now; // 记录开始时间
    SEGENV.aux0 = 0;         // 重置序列步骤为0
    SEGENV.aux1 = 0;         // 重置循环计数器为0
    SEGMENT.fill(BLACK);     // 清除所有像素
    
    // 立即显示序列中的第一个LED
    if (SEGLEN > sequence[0]) {
      SEGMENT.setPixelColor(sequence[0], SEGCOLOR(0));
    }
    
    return FRAMETIME; // 立即返回，确保状态正确初始化
}
```

### 2. RS485地址范围扩展 (1-99 → 1-255) ✅

#### 改进内容
- 将设备地址范围从1-99扩展到1-255
- 支持更多设备在同一RS485总线上工作
- 改进了地址格式化，支持3位数地址显示

#### 地址格式
- **1-9**: `@001`, `@002`, ..., `@009`
- **10-99**: `@010`, `@011`, ..., `@099`  
- **100-255**: `@100`, `@101`, ..., `@255`

#### 开销分析
- **内存开销**: 无增加（`uint8_t`变量已支持0-255范围）
- **处理开销**: 无增加（简单整数比较，O(1)复杂度）
- **通信开销**: 微乎其微（地址字符串长度略增）

## 🔧 技术规格

### 编译信息
- **WLED版本**: 0.16.0-alpha
- **平台**: ESP32
- **编译时间**: 34.12秒
- **内存使用**: 
  - RAM: 24.6% (80,612 / 327,680 bytes)
  - Flash: 84.1% (1,323,341 / 1,572,864 bytes)
- **用户模块**: 2个已注册

### 自定义序列效果规格
- **效果ID**: 218 (FX_MODE_CUSTOM_SEQUENCE_1)
- **效果名称**: "Custom Seq 1"
- **序列模式**: 1→3→5→7→9→8→6→4→2 (基于1的索引)
- **参数控制**:
  - Speed: 控制切换间隔 (100ms - 5.2s)
  - Intensity: 控制拖尾效果 (>128启用)
  - Color: 设置LED颜色

### RS485通信规格
- **设备地址**: 1-255 (可配置)
- **广播地址**: @0 (所有设备执行，不回复)
- **单播地址**: @001-@255 (指定设备执行并回复)
- **波特率**: 9600 (默认，可配置)
- **数据格式**: 8N1
- **引脚配置**: RX=26, TX=27 (默认，可配置)

## 📋 支持的命令

### 基本命令
- `@XXX PRESET <1-250>` - 激活指定预设
- `@XXX PRESET?` - 查询当前预设
- `@XXX STATUS` - 查询设备状态
- `@XXX ADDRESS?` - 查询设备地址
- `@XXX ECHO <text>` - 回显测试
- `@XXX HELP` - 显示帮助信息

### 地址格式示例
```
@001 PRESET 3    # 设备1激活预设3
@010 STATUS      # 设备10查询状态
@255 PRESET?     # 设备255查询当前预设
@000 PRESET 1    # 广播：所有设备激活预设1（不回复）
```

## 🧪 测试验证

### 自定义序列效果测试
1. **网页界面测试**:
   - 激活包含"Custom Seq 1"的预设
   - 切换到其他预设后再次激活
   - 验证序列从LED 1开始

2. **RS485命令测试**:
   - 发送`@001 PRESET 3`激活序列效果
   - 等待几个循环后重新发送命令
   - 验证每次都从序列开始位置重新开始

### RS485地址测试
1. **地址范围测试**:
   - 配置设备地址为1, 99, 100, 255
   - 验证所有地址都能正确响应
   - 测试地址格式化正确性

2. **通信测试**:
   - 测试单播命令（有回复）
   - 测试广播命令（无回复）
   - 验证地址过滤功能

## 📁 更新文件列表

### 核心文件
- `wled00/FX.cpp` - 自定义序列效果修复
- `usermods/rs485_control/rs485_control_simple.cpp` - RS485地址扩展

### 文档文件
- `自定义序列效果测试指南.md` - 测试指南
- `test_custom_sequence_reset.py` - 自动化测试脚本
- `.kiro/project_knowledge_base.md` - 项目知识库更新

### 固件文件
- `build_output/release/WLED_0.16.0-alpha_ESP32.bin` - 更新后的固件

## 🚀 升级指南

### 1. 固件升级
1. 下载最新固件：`WLED_0.16.0-alpha_ESP32.bin`
2. 通过WLED网页界面上传固件
3. 等待设备重启完成

### 2. 配置验证
1. 进入Config→Usermods页面
2. 确认RS485Control模块已启用
3. 检查设备地址设置（现在支持1-255）

### 3. 功能测试
1. 创建包含"Custom Seq 1"效果的预设
2. 测试RS485命令控制
3. 验证序列效果重置功能

## ⚠️ 注意事项

### 兼容性
- 向后兼容旧的RS485命令格式
- 无地址前缀的命令仍按广播模式处理
- 现有预设和配置保持不变

### 地址冲突
- 确保同一RS485总线上的设备地址唯一
- 建议使用连续的地址编号便于管理
- 地址0保留用于广播命令

### 性能影响
- 新功能对系统性能影响微乎其微
- 内存使用量基本无变化
- 通信延迟无明显增加

## 📞 技术支持

如有问题，请参考：
1. `自定义序列效果测试指南.md` - 详细测试步骤
2. `RS485控制模块详细说明.md` - 模块使用说明
3. `WLED二次开发完整指南.md` - 开发指南
4. `.kiro/project_knowledge_base.md` - 项目知识库

---

**版本**: WLED RS485 V5  
**编译日期**: 2025-01-24  
**固件大小**: 1.32MB  
**支持平台**: ESP32