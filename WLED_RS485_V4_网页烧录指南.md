# WLED RS485 V4 网页烧录指南

## 📦 固件信息

- **固件文件**: `build_output/release/WLED_0.16.0-alpha_ESP32.bin`
- **固件大小**: 1.27 MB
- **目标平台**: ESP32
- **WLED版本**: 0.16.0-alpha
- **RS485控制**: ✅ 已集成

## 🔧 烧录步骤

### 1. 准备工作
- 确保ESP32设备已连接到电脑
- 打开WLED设备的网页界面
- 确保设备有足够的存储空间

### 2. 网页烧录
1. 在WLED网页界面中，进入 **Config** → **Security & Updates**
2. 找到 **Manual OTA Update** 部分
3. 点击 **Choose File** 按钮
4. 选择固件文件: `WLED_0.16.0-alpha_ESP32.bin`
5. 点击 **Update!** 开始烧录
6. 等待烧录完成（约2-3分钟）
7. 设备将自动重启

### 3. 验证安装
烧录完成后，验证RS485控制模块是否正确安装：

1. 进入 **Config** → **Usermods** 页面
2. 查找 **RS485Control** 配置选项
3. 如果看到RS485Control配置，说明安装成功

## ⚙️ RS485控制配置

### 默认配置参数
- **启用状态**: 已启用
- **波特率**: 9600
- **RX引脚**: GPIO26
- **TX引脚**: GPIO27
- **缓冲区大小**: 512字节
- **超时时间**: 1000毫秒
- **回声服务**: 已启用
- **调试模式**: 已禁用

### 配置步骤
1. 进入 **Config** → **Usermods**
2. 找到 **RS485Control** 部分
3. 根据硬件连接调整以下参数：
   - **RX Pin**: 接收数据引脚（默认GPIO26）
   - **TX Pin**: 发送数据引脚（默认GPIO27）
   - **Baud Rate**: 串口波特率（默认9600）
4. 点击 **Save** 保存配置
5. 设备将自动重启应用新配置

## 🔌 硬件连接

### ESP32引脚连接
```
ESP32          RS485模块
GPIO26    →    RO (接收输出)
GPIO27    →    DI (数据输入)
3.3V      →    VCC
GND       →    GND
```

### RS485模块连接
```
RS485模块      外部设备
A+        →    A+
B-        →    B-
```

## 📡 支持的命令

### 基本命令格式
所有命令通过RS485串口发送，以换行符结束。

### 命令列表

#### 1. 回声测试
```
ECHO <文本内容>
```
**示例**: `ECHO Hello World`
**响应**: `Hello World`

#### 2. 预设控制
```
PRESET <预设编号>
```
**示例**: `PRESET 1`
**响应**: `OK: Preset 1 activated`
**范围**: 1-16

#### 3. 查询当前预设
```
PRESET?
```
**响应**: `CURRENT: Preset 1`

#### 4. 状态查询
```
STATUS
```
**响应**: `STATUS: Online, Connected: Yes`

#### 5. 帮助信息
```
HELP
```
**响应**: `COMMANDS: ECHO <text>, PRESET <1-16>, PRESET?, STATUS, HELP`

## 🔍 故障排除

### 问题1: Config页面找不到RS485Control
**解决方案**:
1. 确认使用的是正确的固件文件
2. 检查烧录是否成功完成
3. 尝试重启设备
4. 清除浏览器缓存后重新访问

### 问题2: RS485通信无响应
**解决方案**:
1. 检查硬件连接是否正确
2. 确认波特率设置匹配
3. 检查RX/TX引脚配置
4. 启用调试模式查看日志

### 问题3: 命令执行失败
**解决方案**:
1. 确认命令格式正确
2. 检查预设编号范围（1-16）
3. 确保命令以换行符结束
4. 查看错误响应信息

## 📊 状态监控

### 在Info页面查看状态
进入WLED主页面，在Info部分可以看到：
- **RS485 Control Active**: 模块运行状态
- **Messages RX**: 接收消息数量
- **Messages TX**: 发送消息数量
- **Parse Errors**: 解析错误数量
- **Baud Rate**: 当前波特率
- **RX Pin**: 接收引脚
- **TX Pin**: 发送引脚

### 启用/禁用控制
在Info页面点击 **Enable/Disable** 按钮可以快速启用或禁用RS485控制功能。

## 🔧 高级配置

### 调试模式
启用调试模式可以在串口监视器中查看详细的通信日志：
1. 在RS485Control配置中启用 **Debug Mode**
2. 连接串口监视器（波特率115200）
3. 观察RS485通信日志

### 性能优化
- **缓冲区大小**: 根据应用需求调整（默认512字节）
- **超时时间**: 根据网络延迟调整（默认1000毫秒）
- **循环频率**: 系统自动限制为100Hz以优化性能

## 📝 版本信息

- **编译时间**: 2025年1月23日
- **固件版本**: WLED 0.16.0-alpha with RS485 Control
- **模块版本**: RS485 Control v1.0 (Simplified)
- **支持平台**: ESP32
- **内存使用**: RAM 24.6%, Flash 84.0%

## 🆘 技术支持

如果遇到问题，请提供以下信息：
1. 使用的固件版本
2. ESP32硬件型号
3. RS485硬件连接图
4. 错误信息或异常行为描述
5. 串口调试日志（如果启用了调试模式）

---

**注意**: 此固件为简化版本，专门针对网页烧录优化，去除了复杂的类依赖关系，确保编译稳定性和运行可靠性。