# 自定义序列效果测试指南

## 测试目的
验证自定义序列效果"Custom Seq 1"在每次调用预设时都能从头开始执行，而不是继续之前的状态。

## 修复内容
修改了`mode_custom_sequence_1()`函数，确保：
1. 当`SEGENV.call == 0`时正确重置所有状态变量
2. 立即显示序列中的第一个LED
3. 每次效果激活时都从序列开始位置重新开始

## 测试步骤

### 1. 准备工作
- 确保已烧录最新固件（包含修复）
- 确保RS485控制模块已启用
- 创建一个包含"Custom Seq 1"效果的预设

### 2. 创建测试预设
1. 打开WLED网页界面
2. 进入Effects页面
3. 选择"Custom Seq 1"效果（效果ID: 218）
4. 调整参数：
   - Speed: 128（中等速度）
   - Intensity: 200（启用拖尾效果）
   - Color: 选择一个明显的颜色（如红色）
5. 保存为预设3（或其他编号）

### 3. 测试序列重启功能

#### 测试A：通过网页界面
1. 激活预设3，观察LED序列：1→3→5→7→9→8→6→4→2
2. 等待序列执行几个循环
3. 切换到其他预设（如预设1）
4. 再次激活预设3
5. **验证点**：序列应该从LED 1开始，而不是从之前停止的位置继续

#### 测试B：通过RS485命令
1. 使用串口工具连接到RS485
2. 发送命令：`@01 PRESET 3`
3. 观察LED序列从头开始
4. 等待几个循环后，发送：`@01 PRESET 1`
5. 再次发送：`@01 PRESET 3`
6. **验证点**：每次调用预设3时，序列都从LED 1开始

### 4. 测试参数调整
1. 在效果运行时调整Speed参数
2. 调整Intensity参数（控制拖尾效果）
3. **验证点**：参数变化应该立即生效，但序列位置保持连续

### 5. 预期结果
- ✅ 每次激活预设时，序列从LED 1开始
- ✅ 序列按照 1→3→5→7→9→8→6→4→2 的顺序执行
- ✅ Speed参数控制切换间隔（100ms到5.2s）
- ✅ Intensity > 128时显示拖尾效果
- ✅ RS485命令`@01 PRESET X`正确触发效果重启

## 技术细节

### 修复前的问题
- `SEGENV.call == 0`检查后没有立即返回
- 状态变量在效果重新激活时没有正确重置
- 导致效果从之前的状态继续执行

### 修复后的改进
```cpp
if (SEGENV.call == 0) {
    SEGENV.step = strip.now; // 记录开始时间
    SEGENV.aux0 = 0;         // 重置序列步骤为0
    SEGENV.aux1 = 0;         // 重置循环计数器为0
    SEGMENT.fill(BLACK);     // 清除所有像素
    
    // 立即显示序列中的第一个LED
    if (SEGLEN > sequence[0]) {
      SEGMENT.setPixelColor(sequence[0], SEGCOLOR(0));
    }
    
    return FRAMETIME; // 立即返回，确保状态正确初始化
}
```

### 序列定义
```cpp
// 序列：1,3,5,7,9,8,6,4,2（基于1的索引）
// 转换为基于0的索引：0,2,4,6,8,7,5,3,1
static const uint8_t sequence[] = {0, 2, 4, 6, 8, 7, 5, 3, 1};
```

## 故障排除

### 如果序列不从头开始
1. 检查固件是否正确烧录
2. 验证预设是否正确保存了"Custom Seq 1"效果
3. 检查RS485通信是否正常

### 如果序列顺序不正确
1. 确认LED条的长度至少有9个LED
2. 检查LED条的连接和供电

### 如果RS485命令无响应
1. 检查设备地址配置
2. 验证波特率设置（默认9600）
3. 确认RX/TX引脚配置正确

## 编译信息
- 编译时间：2025-01-24
- 固件版本：WLED 0.16.0-alpha
- 内存使用：RAM 24.6%, Flash 84.1%
- 用户模块：2个已注册
- 输出文件：`build_output/release/WLED_0.16.0-alpha_ESP32.bin`